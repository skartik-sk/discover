# Convex Backend

This directory contains all backend functions for the Discover platform.

## Setup

The Convex deployment URL is already configured in `.env.local`.

Run `bunx convex dev` to start development and sync functions.
